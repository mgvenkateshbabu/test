name: ENV Variables
on: push

jobs:
  read_Env_var:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: get a env variable
        run: |
          echo ${{ secrets.token }}
          echo ${name}
          


